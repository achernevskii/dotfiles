---
- include_tasks: install-darwin.yaml
  when: ansible_os_family == 'Darwin'

- name: Sync OpenCode skills
  include_role:
    name: agent_skills
  vars:
    agent_skills_dest: "~/.config/opencode/skills"
