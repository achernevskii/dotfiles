---
- include_tasks: install-darwin.yaml
  when: ansible_os_family == 'Darwin'

- name: Sync Claude Code skills
  include_role:
    name: agent_skills
  vars:
    agent_skills_dest: "~/.claude/skills"
